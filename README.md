# Data-Structure

stacks-it's a data structure that follows a particular order in which the  operations are performed.

 tuples-it's a data structure that consists of an ordered collection of elements.
